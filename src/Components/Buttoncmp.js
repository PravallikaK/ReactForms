import React from "react";

const Buttoncmp=(props)=>{
    return(
        <div>
            <input type="button" value={props.name} onClick={this.handlefav} />
        </div>
    );
}
export default Buttoncmp;